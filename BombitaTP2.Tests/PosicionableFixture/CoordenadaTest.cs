using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using NUnit.Framework;
using BombitaTP2.ObjetosAlmacenables;
using BombitaTP2.Personajes;
using BombitaTP2.Armamento;
using BombitaTP2;
using BombitaTP2.Escenario;
using BombitaTP2.Obstaculos;
using BombitaTP2.Exceptions;

namespace BombitaTP2.Tests.CoordenadaFixture
{/*
    [TestFixture]
    public class CoordenadaTest
    {
        Coordenada _coordenada1;
        Coordenada _coordenada2;
        Coordenada _coordenada3;

        [SetUp]
        public void SetUp()
        {
            // Arrange
            _coordenada1 = new Coordenada(1,3);
            _coordenada2 = new Coordenada(3, 4);
            _coordenada3 = _coordenada1;

        }
        
        [Test]
        public void TestGuardarEnDisco()
        {
            //Assert
            if(!this._coordenada1.NodoProcesado)
                this._coordenada1.Guardar();
            
            if (!this._coordenada2.NodoProcesado)
                this._coordenada2.Guardar();
            
            if (!this._coordenada3.NodoProcesado)
                this._coordenada3.Guardar();
        }

        [Test]
        public void TestRecuperar()
        {
            // Arrange
            Assert.IsInstanceOf(typeof(Molotov), _bombita.Armamento);

            // Act
            _bombaToleTole.AplicarEfecto(_bombita);

            // Assert
            Assert.IsInstanceOf(typeof(ToleTole), _bombita.Armamento);
  
      }

        
    }
    */
}
